{
  "name": "advent-of-code-2024",
  "version": "1.0.0",
  "description": "",
  "main": "npm run compile && AOC_DEMO=false ./runDay.sh",
  "scripts": {
    "day": "npm run compile && AOC_DEMO=false ./runDay.sh",
    "day:new": "./newDay.sh",
    "day:demo": "npm run compile && AOC_DEMO=true ./runDay.sh",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "./build.sh",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.3",
    "gts": "^6.0.2",
    "@types/node": "^22.7.5"
  }
}
